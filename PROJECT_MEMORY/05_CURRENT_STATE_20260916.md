# 현재 상태 스냅샷

기준일: 2026-09-16 (Asia/Seoul)

## 코드·규정

- 정본 규정: `../RULES.md`
- 사이트 설명과 핵심 원칙: `../README.md`
- 작업 구현: `../worker-blog.mjs`, `../brief.mjs`, `../cards.mjs`, `../check.mjs`, `../build.mjs`
- 규정의 핵심은 승인 전 제휴·광고 차단, 검증되지 않은 값 금지, 얇은 글·키워드 남용·대량 자동 발행 금지다.

## 실행 상태

- 블로그 worker health: `fallback_ready`
- 기존 생성기 실행은 조직 정책으로 차단되어 있으며, Codex 대체 제작 경로가 준비되어 있다.
- 이 상태는 생성기 장애를 뜻하며, 콘텐츠 품질 승인이나 플랫폼 승인을 뜻하지 않는다.

## 원고 상태

원격 review 기록 기준으로 다음 원고들이 검수 대기 상태다.

- 식기세척기 12인용·14인용 비교 기준
- 유모차 선택 기준
- 도마 선택 기준
- 에너지 1등급 확인 기준
- 젖병 선택 기준

현재 확인된 것은 `review` 상태이지 `approved` 또는 `published` 상태가 아니다. 사용자 최종 검수 전에는 외부 발행으로 간주하지 않는다.

## 확인이 필요한 외부 상태

- Google AdSense와 네이버의 심사 상태는 실제 이메일·계정 화면으로 다시 확인해야 한다.
- 승인 여부를 대시보드 문구만으로 단정하지 않는다.
- 수익·방문자·전환 데이터는 실제 분석 계정의 관측값이 쌓인 뒤 판단한다.

## 2026-09-16 대시보드·Gmail 직접 확인

- 대시보드 `블로그&구글` 탭 표시: `AdSense 심사 결과를 기다리는 중`
- 대시보드 하단 표시: 네이버·구글 모두 심사 결과 대기. 단, Gmail과 불일치하는 낡은 표시
- Google 색인: 0, Search Console 연결 필요로 표시
- Naver 색인: 0, 신규 사이트는 2~4주로 표시
- 메일 감시: 대시보드에는 마지막 기록 59시간 전으로 표시됨
- Gmail 직접 확인: Google AdSense가 2026-09-15 오전 10:26에 이전 심사 결과로 `애드센스를 사용하려면 사이트에서 발견된 문제를 수정해야 합니다`라는 메일을 보냄
- AdSense 계정 직접 확인: 사이트 상태 `준비 중`, `사이트의 광고 게재 가능 여부 검토 중`
- AdSense 계정 직접 확인: `리뷰가 요청됨`, 요청 시간 `2026-09-15 19:45`, Google 안내상 일반적으로 며칠, 경우에 따라 2~4주 소요
- 결론: 최신 상태는 `이전 거절 후 수정·재검토 요청 완료, 현재 재검토 대기`다. 대시보드의 대기 표시는 현재 계정 화면과 일치하지만 메일 감시 시각은 낡아 있다.

## 최신 갱신 — 2026-09-16 22:17 KST

- Gmail read-only OAuth 연결 완료: `billy5285@gmail.com`.
- Gmail API에서 AdSense 관련 메일 3건을 실제 조회했고, 3시간 주기 launchd 감시기를 등록했다. 서비스 설정은 `run interval=10800`, `last exit code=0`이며 one-shot 실행 후 종료되는 구조다.
- 대시보드에서 `메일 감시 확인됨 · 마지막 기록 5분 전`을 브라우저로 확인했다.
- 원격 검수 QA는 5/5, 로컬 자동 측정은 44/44이며 화면 사실성 수동 검수 1건이 남아 있다.
- 검색·분석 계정 연결은 아직 확인되지 않았으므로 방문자·수익 숫자를 만들거나 추정하지 않는다.
- 공식 전체 공정률은 6개 게이트 중 5개 통과로 `83.3%`다. 콘텐츠 worker는 Claude 조직 제한으로 Codex 대체 제작 대기 상태다.
- 최신 배포 전 규정 검사 재실행 결과는 `27/27 통과`, 실패 0이다. E5 화면 사실성은 사람 검수 항목으로 남겨 두었다.
- Search Console 속성 연결을 확인했고, 블로그&구글 전용 GA4 속성·웹 스트림을 생성했다. 측정 ID를 코드와 gh-pages 산출물에 반영했으며 첫 데이터 수집만 대기 중이다.
- GitHub Pages 재빌드 완료, 공개 URL HTTP 200 및 GA4 측정 ID 노출을 확인했다. 실측 방문자·노출 수치는 데이터가 쌓이기 전까지 미측정으로 유지한다.

## 최신 갱신 — 2026-09-16 22:55 KST

- 승인 직후 발행 E2E 드라이런을 실제 운영 저장소·공개 사이트로 실행했다: `9/9 통과`, 미흡 0.
- 드라이런은 GitHub 쓰기·광고 삽입·실제 발행을 호출하지 않았고, 사용자 승인 잠금 및 승인 전 제휴 링크 차단을 확인했다.
- 이미지 권리 대장을 추가하고 카드 16장을 프로젝트 내부 생성 자산으로 기록했다. 외부 사진·로고·초상은 사용하지 않는다.
- 운영 대시보드에 이미지 권리 게이트를 반영하고 브라우저에서 `6/6`, `첫 실측 데이터 대기`, `16장 내부 생성 자산 확인`을 직접 확인했다.
- 내부 준비 공정은 `6/6 = 100%`로 완료했다. 외부 AdSense 심사 결과와 첫 GA4 실측 데이터는 대기 상태이며 이를 완료로 계산하지 않는다.

## 승인·대시보드 상태 기록 원칙

- 이 문서의 기준일 현재 승인 상태는 이 기록만으로 확정하지 않는다.
- `pending`, `approved`, `rejected`, `unknown`을 구분하고, 계정 화면 또는 이메일 증거가 없으면 `unknown`으로 둔다.
- 대시보드 연결 전에는 연동 완료라고 보고하지 않는다.
