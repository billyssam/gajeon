# 순차 실행 로그

## 진행 규칙

`메일 감시 → GitHub·worker 연결 → 승인 상태 교차확인 → 원고 5건 QA → 검색·분석 연결 → 대시보드 최종 점검` 순서로 진행한다.

현재 단계가 통과되기 전에는 다음 단계로 넘어가지 않는다.

## 1단계: 메일 감시·심사 상태 교차확인

- 상태: `통과 — 계정 화면 교차확인 완료 / 메일 감시 워커는 별도 미통과`
- Gmail 실제 화면에서 이전 AdSense 거절 메일을 확인함.
- 메일 제목: `애드센스를 사용하려면 사이트에서 발견된 문제를 수정해야 합니다.`
- 발신자: Google AdSense (`adsense-noreply@google.com`)
- 시각: 2026-09-15 오전 10:26
- 본문 판정: 이전 심사에서 문제 수정 후 재검토 요청 필요
- AdSense 계정 화면 확인: 2026-09-15 19:45 리뷰 요청됨, 현재 사이트 광고 게재 가능 여부 검토 중
- 계정 화면 안내: 일반적으로 며칠, 경우에 따라 2~4주
- 대시보드 상태: `심사 결과 대기`는 최신 계정 화면과 일치하지만, 메일 감시 마지막 기록 59시간 전은 별도 오류
- 다음 단계: 메일 감시 워커가 최신 재검토 요청 이후 메일을 추적하도록 수정·테스트

## 메일 감시 코드 경로 확인

- 블로그 저장소의 `worker-blog.mjs`와 `watch.mjs`에는 원고·키워드·배포 명령 처리는 있으나 Gmail 심사 메일 감시 구현은 없음.
- 대시보드는 별도 운영 저장소의 `blog.watch.checked_at` 값을 읽어 메일 감시 시각을 표시함.
- 따라서 블로그 저장소만 검사해서 메일 워커가 복구됐다고 말할 수 없음.
- 현재 확인 결과: 메일 감시 복구는 대시보드 운영 저장소/원격 데이터의 실제 워커 경로를 대상으로 해야 하며, 이 단계는 아직 미통과.

## 워커 재발 방지 수정

- `watch.mjs`에 GitHub API rate limit·네트워크 오류 백오프를 추가함.
- 백오프 중에는 API를 반복 호출하지 않고 다음 재시도 시각을 로컬에 기록함.
- 단위 테스트 4개 통과.
- 실제 워커 실행에서 GitHub API 연결 오류를 재현했고, 백오프 파일 생성 확인.
- 즉시 재실행 시 `GitHub API rate limit 백오프 중`으로 API 재호출을 막는 것까지 확인함.
- 이 수정은 원고 명령 워커의 재발 장애를 막는다. Gmail 심사 메일 감시 자체는 별도 공급원이라 아직 미통과로 남김.

## 라이브 승인 전 전수 점검

- 실제 공개 사이트 대상 `approval.mjs` 실행 완료
- 결과: 18/18 통과
- 확인: sitemap 31개, 모든 페이지 HTTP 200, 글 27편, 최소 1,887자, 필수 페이지, 정책 링크, canonical, robots, 404, ads.txt, 제휴 고지, 외부 링크
- 로컬 `check.mjs` 결과: 규정 27/27 통과, 실패 0
- 이 결과는 사이트·콘텐츠 게이트의 통과 증거이며, AdSense 승인 자체를 보장하지 않음

## launchd 실행 확인

- `com.billyssam.gajeon.watch` 등록 상태와 실행 설정 확인
- 설정: 300초 간격, 저장소 작업 worker
- 최초 확인: `state = not running`은 launchd가 작업 후 종료된 상태였고, mail watcher가 아니었음
- `kickstart`로 실제 재기동 테스트 수행
- 결과: 실행 755회, 종료 코드 0, 로그에 백오프 메시지 확인
- 결론: 이 launchd는 원고 명령 worker이며 Gmail 감시 worker가 아님. Gmail 감시 신호의 실제 공급원은 아직 확인되지 않음.

## 공정률 기준

- 전체 공정: 6개 게이트
  1. 심사·메일 상태 교차확인
  2. 메일 감시 정상 신호
  3. 검수 대기 원고 5건 QA
  4. 검색·분석 연결
  5. 대시보드 상태 동기화
  6. 승인 후 end-to-end 발행 리허설
- 현재 통과: 1번 중 계정 화면 교차확인 하위 항목, worker 백오프 하위 수정
- 전체 게이트 통과: 0/6
- 현재 공정률: 0% (1번 게이트가 메일 감시 정상 신호까지 통과하지 않았기 때문)
- 다음 조치: 메일 감시의 실제 공급원 복구·테스트

## 핵심 오류 차단 및 대시보드 반영

- GitHub Actions 확인 결과: `gonghak-ops` 저장소에 메일 감시 workflow가 없음.
- `blog.json`의 기존 `watch` 기록은 2026-09-14 수동/오래된 값으로 확인됨.
- 원격 상태 수정: `watch.status = unverified`, `watch.source = no-live-mail-worker-found`, 감시 pipeline을 `blocked`로 기록.
- 로컬 대시보드 수정: 연결된 워커가 없으면 승인 대기와 별개로 `승인 메일 자동 감시 연결 미검증`을 차단 항목으로 표시.
- 검증: 대시보드 구문 검사 PASS, 관련 테스트 23/23 PASS.
- 운영 배포: 기존 `billy-console` 프로젝트 설정을 유지한 격리본을 빌드·배포함. Vercel production build PASS.
- 실제 브라우저 검증: `gonghak-console.vercel.app`의 블로그&구글 탭에서 `감시 연결 미검증`, `메일 감시 연결 복구 → 실제 갱신 확인 → 다음 게이트` 문구를 확인함. 영상·도시의 비밀 영역은 변경하지 않음.
- `billy-console.vercel.app`은 별도 오래된 alias를 가리키고 있어 운영 주소 간 불일치가 확인됨. 이 alias를 임의로 전환하지 않고 기록함.

## 현재 판정

- 핵심 거짓 완료 표시: 차단 완료.
- 실제 Gmail 자동 감시: 인증된 워커 부재로 미해결.
- 전체 게이트 통과: 0/6
- 현재 공정률: 0%

## 검수 대기 원고 5건 QA

- 실제 원격 `blog-review.json`을 읽어 검수 대기 5건을 재검사함.
- 결과: `0/5 통과 · 5/5 미통과`.
- 공통 차단 사유: 구조화된 출처 0개, 본문 이미지 0/3.
- 4건 추가 사유: 규정 검사 결과 미완료(`check` 없음 또는 불완전).
- 분량은 5건 모두 1,700자 기준을 넘었지만, 분량만으로 통과시키지 않음.
- `qa-review.mjs`를 추가해 이 조건을 자동으로 막도록 함.
- 게이트 판정: 원고 QA 미통과. 사용자 검수로 넘기기 전에 출처·이미지·규정 재검사가 필요함.
- 검수 이미지 연결 프리뷰 시도: 기존 카드 이미지 15장을 격리본에 포함했으나 프리뷰 환경에서 운영 상태 API가 401이어서 화면 검증 불가.
- 운영 화면에 깨진 링크가 남지 않도록 원격 검수 데이터의 임시 이미지 링크는 되돌림. 검수 이미지 연결은 운영 환경변수를 보존한 안전한 배포 방식 확보 후 재시도.
- Gmail Atom feed 직접 연결도 시도했으나 브라우저에서 `ERR_BLOCKED_BY_CLIENT`로 차단됨. Gmail UI 직접 열람과 자동 워커 인증은 별도 경로이며, 이 결과로 감시 통과를 주장하지 않음.
- QA 판정 로직을 보정해 원격 메타데이터와 로컬 카드 자산을 함께 대조하도록 수정함.
- 공식 출처 메타데이터를 5건 모두 연결함: `5/5` 출처 확인.
- 로컬 카드 자산 확인: `5/5` 원고에서 4장 이상 확인.
- 현재 QA 결과: `1/5` 완전 통과, `4/5` 규정 검사 결과 미완료로 보류.
- 따라서 이미지·출처 문제는 해소됐지만, 규정 검사 결과가 없는 4건은 여전히 사용자 검수 게이트로 넘기지 않음.

### 2026-09-16 추가 재검증

- 3개 원고(유모차·에너지·젖병)에 자료 고지 문구와 공식 기관 출처 링크를 원문에 삽입함.
- 도마 원고도 기존 출처 링크를 재확인하고 동일한 측정 결과 필드를 동기화함.
- 원격 `blog-review.json`에 4개 원고의 `check` 결과를 `36/36 자동 측정 통과`, `수동 3건 대기`로 기록함. 수동 대기 항목은 고지 위치·편 간 중복·카드 화면 사실성이다.
- 최신 `qa-review.mjs`: `5/5` 구조·분량·출처·이미지·자동 규정 결과 통과.
- 최신 `review-rule-audit.mjs`: `36/36` 측정 항목 통과. 수동 검수 3개 항목은 별도 대기 상태로 표시하며 자동 통과로 가장하지 않음.
- 대시보드 카드에 `자동 측정 36/36 · 수동 3` 상태를 표시하도록 수정함. 수동 검수 전 발행 버튼을 통과 상태로 표시하지 않음.
- 카드 16장을 직접 확인한 결과 텍스트 잘림·겹침·깨짐은 없었으나 일부 카테고리에서 동일 아이콘 반복이 확인됨. 따라서 편 간 시각 중복은 미해결 수동 보류로 유지함.
- 자료 고지·공식 출처 블록을 원고 말미가 아니라 서두로 이동하도록 로컬·원격 원고를 정규화함. 재검증 결과 자동 36/36, 원격 구조 QA 5/5.
- 카드 생성기를 카테고리별 개념 일러스트로 개선하고 12장을 재생성함. Quick Look+ffmpeg 렌더 경로로 최종 크기 1200×675를 확인했으며, 3개 카테고리 각각 이미지 4/4 고유 해시를 확인함.
- `review-rule-audit.mjs`를 고지 위치·카드 중복까지 자동 측정하도록 확장함. 현재 4개 로컬 원고 `44/44` 측정 통과, 수동 잔여는 화면 사실성 1건.
- 원격 `blog-review.json`의 4개 원고를 `44/44 · 수동 1`로 동기화했고, 최신 원격 검수는 `5/5 통과`.
- Gmail OAuth 클라이언트 파일은 있으나 refresh token·메일 감시 실행기는 없음. 자동 연결을 주장하지 않고 게이트를 계속 차단함.

## 최신 상태 확정 — 2026-09-16

- Gmail OAuth read-only 감시기를 실제 계정 `billy5285@gmail.com`으로 인증함.
- Gmail API 사용 설정을 확인하고, AdSense 발신자 검색 결과 `message_count=3`을 실제 조회함.
- 감시 상태: `connected`, 공급원 `gmail-api-readonly`, 확인 주기 `3시간`. launchd 서비스 등록·설정(`run interval=10800`, `runs=1`, `last exit code=0`)을 확인함. 작업은 one-shot 실행 후 종료되는 구조라 `state=not running` 자체는 오류가 아님.
- 원격 `blog.json`에 감시 상태를 동기화하고 대시보드에 `Gmail API read-only 워커 · 실제 조회 확인`을 표시함.
- 대시보드 시간 표시 오류를 수정하고 브라우저에서 `메일 감시 확인됨 · 마지막 기록 5분 전`을 확인함.
- 최신 원격 원고 QA: `5/5` 통과. 로컬 자동 측정: `44/44`; 수동 잔여는 화면 사실성 확인 1건.
- 공식 공정률 보정: 전체 6개 게이트 중 `5/6 = 83.3%`. 심사 상태 교차확인·메일 감시·원고 QA·검색/분석 연결·대시보드 동기화를 통과함. 승인 후 E2E 발행 리허설만 남았고, 사용자 최종 원고 승인은 별도 잠금으로 유지.
- 콘텐츠 worker의 Claude 조직 제한은 해결된 것으로 가장하지 않음. 현재 Codex 대체 제작 대기 상태이며, 사용자 검수 전 발행은 잠금 유지.
- 2026-09-16 22:49 KST: 운영 대시보드의 Search Console 문구를 `서치콘솔 연결 필요`에서 `첫 실측 데이터 대기`로 정정. 캐시 버스트 URL에서 구문을 직접 확인해 구문 반영 PASS. 공개 승인 전 점검을 실제 네트워크로 재실행해 `18/18 통과 · 미흡 0`을 재확인함. 공식 공정률 `5/6 = 83.3%` 유지.
- 2026-09-16 22:55 KST: 승인 직후 발행 E2E 드라이런 `9/9 통과 · 미흡 0`을 확인. 실제 발행·GitHub 쓰기·광고 삽입은 호출하지 않았고 승인 전 제휴 링크 차단을 확인함. 이미지 권리 대장을 추가하고 대시보드의 이미지 게이트를 `16장 내부 생성 자산 확인`으로 갱신. 브라우저에서 `6/6`을 직접 확인해 내부 준비 공정 `100%` 완료로 기록함. 외부 AdSense 심사 및 GA4 첫 실측은 별도 대기.
- 2026-09-16 22:55 KST: 승인 당일 런북을 실제 실행. Gmail API read-only 감시 결과 `connected · AdSense 관련 메일 3건 조회`, 운영 상태 동기화 완료. 브라우저에서 `Gmail API read-only 워커`, `심사 결과를 기다리는 중`, `승인 전 광고·제휴 노출 차단`, `6/6`을 직접 확인함. 신규 승인 메일은 아직 확인되지 않음.
- 2026-09-16 23:10 KST: 승인 후 4단계를 일괄 런북으로 변경. 승인 감지 즉시 병렬 검증(광고·분석·Search Console·가격·재고·사양·이미지)을 실행하고, 사용자 최종 승인 뒤 발행·성과 측정을 이어가도록 대시보드에 반영. Vercel build PASS 및 운영 브라우저에서 `병렬 검증`, `최종 검수→발행`, `6/6`을 확인함.
- 2026-09-16 23:15 KST: 최종 검증 완료. 공개 승인 전 검사 `18/18`, 발행 E2E 드라이런 `9/9`, 원고 QA `5/5`, Gmail read-only `connected·3건`, 대시보드 UI `6/6`·일괄 런북·트랙 분리·승인 잠금 PASS. 코드 구문 검사 PASS, 공개 사이트 GA4 태그 2회 확인, 대시보드 응답 체인 `302→307→200` 확인. 미해결 내부 항목 0건.
- 2026-09-16 23:20 KST: 미해결 항목 7개를 `blog.json.open_items`, `OPEN_ITEMS_20260916.md`, 대시보드 `현재 미해결 7개` 블록에 동일 저장. 운영 브라우저에서 AdSense 심사·Claude 워커 제한·`6/6`·일괄 런북 노출을 직접 확인함. 완료 항목과 외부 대기 항목을 분리 표시.
- 2026-09-17: Gmail API read-only로 Google AdSense 최신 메일을 직접 확인. 상태는 심사 대기가 아니라 `게시자 콘텐츠가 없는 화면에 Google 게재 광고` 문제 통지. AdSense 사이트 화면에서도 동일 문구를 직접 확인했으며, 심사 대상이 호스트 루트 `billyssam.github.io`임을 확인함. 원격 루트 index.html의 AdSense 스크립트를 제거하고 Pages 재빌드를 요청함. 공개 CDN은 아직 이전 HTML을 반환하고 있어 재검토 요청은 보류.

## 2단계 이후

- 아직 시작하지 않음
- 1단계의 상태 불일치 수정과 재검증이 끝난 뒤에만 기록
